- ssh -l thowach 192.xxx.xx
- ssh thowach@192.168.xx.x.
- ssh -p 2464 thowach@192.168.27.20  = mit neuer Port Nummer
- ip addr sh
- ./VBoxaddition.run
- apt-get install net-tools
- apt dist-upgrade
- sudo apt-get purge cinnamon    =  "apt purge" löscht auch Zusatzpakete
- nano /boot/grub/grub.cfg 
- grub customizer
- sudo deluser --remove-home thowach
- sudo deluser --remove-all-files
- 😊️ cat /etc/group = zeigt alle groups
- 😎️ cat /etc/shadow = zeigt alle user
- id thowach2
- chmod +x datei.sh
- apt-get autoremove --purge gnome*
- apt(-cache) search [php]
- apt-cache depends [Paket] 
- cp -r  = kopieren
- ls -R = listet auch Unterordner auf
- STRG +U = alle zeichen vor dem Curser löschen
- vim Editor =  shift +7 suchen
  dd = Zeile löschen
- mkdir -p Ordner/file2/file3/ = erstellt auch die Unterordner
- touch test1.txt test2.txt.test3.txt = (minus -p), wie bei mkdir ist bei dem touch-Befehl nicht nötig.
- man hier = hierarchie (erklärt Systemordner) 
- dpkg -l | less = listet installierte Programme auf
- du -h *.txt = Größe von Dateityp anzeigen
- sudo useradd -m -s /bin/bash -c "Kommentar" thowach
- LESS = ansehen/ mit V in Editor wechseln
  sudo update-alternatives --config editor = standard Editor ändern
- tail -n 3 <Dateipfad>	Zeigt die letzten 3 Zeilen an
- head -n 3 = zeigt die ersten 3 Zeilen an.
- source ~/.bash_aliases = ZUR ERINNERUNG 
- ps -u thowach = Zeigt laufende Prozesse des Benutzers 
- glxgears = Grafikkartenfunktions-Anzeige
- du -cksh target_name = Ordner oder einer Datei schnell berechnen
- 😊️ du -cksh = Ordners oder einer Datei schnell berechnen 
- unzip Datei.zip -d Zielordner
• strg+alt Pfeiltaste rechts = Arbeitsfläche wechseln
• 😎️ df -h = Festplattenbelegung gesamt
• du -sh ./ = Festplattenbelegung aktuelles Verzeichnis
• apt list --installed = alle installierten Programme auflisten
  dpkg -l > programme = legt zusätzlich die Datei "Programme" an
  dpkg --get-selections | grep Programm
• grep " install " /var/log/dpkg.log = Zuletzt install. Progrogramme mit Datum
• /etc/timeshift.json 
• sudo nemo = Dateimanager mit erhöhten Rechten öffnen
- Taste F3 = Dateimanager, bzw. Fenster doppelt
• lsb_release -a = Linux Version anzeigen
- sudo usermod -aG vboxsf thowach (für gemeinsamen Ordner in VB)
- sudo usermod -a -G sudo thowach = Benutzer thowach der Gruppe sudo zufügen; thowach ist nun "sudo berechtigt"
- 😀️ systemctl status sshd.service 
- sudo service ssh status
- VBoxManage modifyhd "/pfad/zum/volume.vdi" --resize 51200 
- sudo hddtemp /dev/sda  oder /sdb usw.  = zeigt Festplattentemperatur an
- sudo dmidecode -t 2  = Mainboard Model anzeigen
- lsblk -f = UUID anzeigen
- sudo blkid /dev/sdb
- Midnight Comander öffnen = mc
- ps aux | grep -i apt = zeigt apt Prozesse an 
- sudo fdisk /dev/sdb 
- lsblk -f
- sudo mkfs.ext4 /dev/sdb1
- sudo mount /dev/sdb1  /media/thowach/newdisk    (newdisk Ordner vorher erstellen)
- UUID=XYZABCXYZ  /media/thowach/newdisk    ext4  (oder ntfs bei windows)   defaults   0  2   = fstab Datei Eintrag
- sudo apt install -f   = versucht beschädigte Pakete zu reparieren
- fsck[option] = prüft/repariert das Dateisystem
- find / -name "wine" > /home/thowach/find.txt  = trägt die gesuchten Daten in die find.txt Datei ein
- ranger = Filemanager im Terminal
- thowach@thowach:~/Downloads/ventoy-1.0.53$ sudo ./VentoyWeb.sh          = ventoy im Browser öffnen
- ALT + F2 - r = Cinnamon restart
- STRG + ALT + F2 - login im Terminal, oder STRG + ALT + F7 = zurück
- cinnamon-session-quit --logout --no-prompt  = abmelden
- STRG + ALT + li./re. Pfeiltaste = Arbeitsflächen wechseln
- netstat -tlpn = Netzwerk-Dienste identifizieren
- sha256sum  datei.ova
- sudo openvpn /home/thowach/Downloads/tryhackme/thomas58.ovpn 
- cat /etc/hosts | grep localhost
- man usermod | grep -- -G   = (ACHTUNG 2 Bindestriche -- vor -G)zeigt den usermod -G Befehl an
-🤔️ man chown | grep -- -R   
- man:mkdir in url Leiste eingeben =  manpage in Firefox öffnen/lesen
- cat /var/log/apt/history.log  = zuletzt installierte/entfernte Programme
- convert Bild1.png Bild2.jpg
- dpigs -n 30 -H  =   zeigt die Programmgröße, von hier 30 Programmen aufgelistet und in Größenreihenfolge sortiert
- nano -c = Zeilen anzeigen
- rm  *.copy   = ALLE .copy Dateien werden gelöscht
- Strg + L = löscht den Bildschirm (Terminal)
- command | less
- free -h  = Swap-Speicher ansehen
- w3m Bild.jpg  = Bilder im Terminal anzeigen
- #!/usr/bin/env python3    =    (.sh Script-shebang) im PythonScript eingeben im Atom-Editior
- sudo netstat -nap | grep :80
 sudo kill xxx
- sudo /etc/init.d/ mysql (auch apache2) stop
- export PATH="to-your-dir:$PATH"
echo $PATH prüfen
- curl ifconfig.me  = öffentliche IP Adresse anzeigen
- sudo hostnamectl set-hostname   = Hostname festschreiben (nach Eintrag in /etc/hosts und hostname)
- In der manpage: / (und Suchbegriff) eingeben
- timedatectl list-timezones, 
  timedatectl set-timezone Europe/Berlin
- ls /sys/firmware/efi/  = Linux efi oder bios erkennen
- chown -R thowach:thowach . (Punkt beachten) der Punkt ändert den owner (zb./www/html) in thowach anstatt den ganzen Pfad anzugeben
- sudo find /home/thowach/Bilder/ -name "*.jpg"   
  locate -i --regex "ubuntu(.*)\.ISO"
- echo "<?php phpinfo(); ?>" | sudo tee /var/www/html/info.php    = tee erstellt die php.info Datei 
- saa --purge wine*
- sftp://remote_ipadress
- ls /etc/cron*  = alle Dateien die mit cron beginnen auflisten
- cvlc v4l2:///dev/video0 = Webcam Command 
- dpkg -l | grep cups
- strg + 6 = auswählen (Nano)
- Alt + 6 =  kopieren (Nano)
- strg + u = einfügen  (Nano)
- du -hs /home/thowach/Virtualbox      =  zeigt belegten Speicherplatz an
- ps ax | grep ssh
ps -e    = Prozesse anzeigen
- free -th   = RAM anzeigen
- cat /var/log/auth.log    ---- auth.log Berich ansehen
1)- usermod -l NEUERNAME ALTERNAME    = Benutzernamen ändern
2)  usermod -d /home/NEUERNAME -m NEUERNAME  
Bei: Benutzer wird derzeit von Prozess 6886 verwendet:
3)- pkill -u username pid 
    pkill -9 -u username 
- usermod -l NEUERNAME ALTERNAME
- 
----------------------------------------------------------------------------------------------------------------------
- REDSHIFT: www.latlong.net
-redshift -l 53.7513549:9.663252100000022
-redshift -t 6000:3500
-cd ~/.config/(redshift.conf)
----------------------------------------------------------------------------------------------------
STRG & SHIFT & li.Maustaste = Datei/Ordner Verknüpfung erstellen
SHIFT & BILD Pfeil up/down = VB- Terminal-Fenster auf/ab scrollen 
------------------------------------------------------------------------------------------------------
• gs -sDEVICE=pdfwrite -dNOPAUSE -dBATCH -sOutputFile=test.pdf test.pdf = PDF Dateien komprimieren
-                 ~/pdfsizeopt/pdfsizeopt input.pdf output.pdf = PDF Dateien komprimieren
---------------------------------------------------------------------------------------------
- sudo add-apt-repository -r ppa:colingille/freshlight
Die andere Möglichkeit besteht darin sie einzusehen cat /etc/apt/sources.list   und  cd /etc/apt/sources.list.d/ von dort zu entfernen.
Übersicht der aktivierten Quellen:   grep -P '^[ \t]*[^#[ \t]+' /etc/apt/sources.list /etc/apt/sources.list.d/*.list 


sudo add-apt-repository --remove ppa:whatever/ppa
Als sicherere Alternative können Sie ppa-purge installieren:

sudo apt-get install ppa-purge
Entfernen Sie anschließend die PPA, und stufen Sie die bereitgestellten Pakete ordnungsgemäß auf Pakete herunter, die von offiziellen Repositorys bereitgestellt wurden:

sudo ppa-purge ppa:whatever/ppa
Beachten Sie, dass hierdurch die von der PPA bereitgestellten Pakete deinstalliert werden, nicht jedoch die von den offiziellen Repositorys bereitgestellten. 
Wenn Sie sie entfernen möchten, sollten Sie apt mitteilen:

sudo apt-get purge package_name
Sie können PPAs auch entfernen, indem Sie die .listDateien aus dem /etc/apt/sources.list.d Verzeichnis löschen .

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
1) wenn ich mich recht erinnere, habe ich das Verzeichnis /var/www/ einfach per chown und chgrp meinem Benutzer zugeordnet. 
2) Und in der Apache Config habe ich ebenfalls meinen User/meine Gruppe zum Standard für neue Dateien und Ordner gemacht.
                  sudo chown -R	thowach:thowach   /var/www/html	 

1) sudo chown -R www-data:www-data   oder   root:root  oder  thowach:thowach     var/www/html	  
   ändert den Ordner, Inhalt und alle Unterordner von „/var/www“ auf den Besitzer und Gruppe“www-data“
2) Benutzer in die Gruppe des Webservers aufnehmen und ihnen darüber Schreibrechte zu gewähren:
    sudo usermod -a -G www-data [User] bzw.
	sudo usermod -a -G thowach www-data  =  = Benutzer www-data in die Gruppe thowach aufnehmen

3)   sudo chmod -R g+w /var/www/html = Schreibrechte geben = WICHTIG!

-------------------------------------------------------------------
sudo chown -R	thowach:thowach   /var/www/html	 
sudo chmod -R g+w /var/www/html
sudo usermod -a -G thowach www-data
sudo systemctl restart apache2

Apache Port 80 belegt (Bei z.B. TYPO 3 Seite) =  sudo nano /etc/apache2/apache2.conf


<Directory /var/www/>
        Options Indexes FollowSymLinks
        AllowOverride None ( in AllowOveride All ändern )
        Require all granted
</Directory>

sudo a2enmod rewrite
sudo systemctl restart apache2

-------------------------------------------------------------------------------------------------------------

md5sum antergos-2016.04.22-x86_64.iso
gpg --keyserver hkp://pool.sks-keyservers.net --search-keys A1AA7A1D
gpg --verify antergos-2016.04.22-x86_64.iso.sig
--------------------------------------------------------------------------------

UEFI install Linux Mint:
https://www.youtube.com/watch?v=JaK1Birq0xM

1) Gparted - Device - create Partition - GPT
2)NEW - FAT32 - New size 300(Mib) - add
3) Speichern
4) Manage Flags (für 300Mib) - boot ankreuzen, esp lassen
5) close, nicht auf speichern gehen
--
6) Install mehr als 30GB (zB.32.720 MB) bei /
  swap (zB.3584 MB) und home anlegen
7) auf Bootloader achten, muss efi sein.

---------------------------------------------------------------------
FLASHPLAYER:

- sudo apt-get install pepperflashplugin-nonfree (Fashplayer)
- sudo update-pepperflashplugin-nonfree –status
- sudo update-pepperflashplugin-nonfree –install (update)

--------------------------------------------------------

sudo /opt/lampp/lampp start
-------------------------------------------

ALIAS ALIAS ALIAS ALIAS

sudo nano .bash_aliases
 source ~/.bash_aliases


alias e='exit'
alias c='clear'
alias ssn='sudo shutdown now'
alias sr='sudo reboot'
alias sai='sudo apt install'
alias sau='sudo apt update'
alias saug='sudo apt upgrade'
alias saa='sudo apt autoremove'
alias saac='sudo apt autoclean'
alias sap='sudo apt purge'
alias alug='apt list --upgradable'
---------------------------------------------------------
Static ip address Ubuntu 20.04:
- sudo cat /etc/cloud/cloud.cfg.d/subiquity-disable-cloudinit-networking.cfg
network: {config: disabled}
- sudo cp /etc/netplan/00-installer-config.yaml /etc/netplan/00-installer-config.yaml.bak
- sudo nano /etc/netplan/00-installer-config.yaml
network:
  ethernets:
    enp0s3:
      dhcp4: no
      addresses:
        - 192.168.27.24/24
      routes:
       - to: default
         via: 192.168.27.1
      nameservers:
          addresses: [8.8.8.8, 1.1.1.1]
  version: 2
- sudo netplan apply
------------------------------------------------------------
- sudo ufw app list
- sudo ufw status
- sudo systemctl restart ufw
- sudo ufw allow ssh
- sudo ufw allow 3541/tcp      = Port eintragen
- sudo ufw deny 3541/tcp    = Port löschen
- sudo service sshd reload
--------------------------------------------------------------

